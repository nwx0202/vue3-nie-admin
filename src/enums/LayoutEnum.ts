/**
 * 菜单布局枚举
 */
export const enum LayoutMode {
    LEFT = 'left',
    TOP = 'top',
    MIX = 'mix'
};

/**
 * 侧边栏状态枚举
 */
export const enum SideBarStatus {
    OPENED = 'opened',
    CLOSED = 'closed'
};

/**
 * 组件尺寸枚举
 */
export const enum ComponentSize {
    DEFAULT = 'default',
    LARGE = 'large',
    SMALL = 'small'
}